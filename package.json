{
  "scripts": {
    "start": "json-server --watch items.json",
    "deploy": "gh-pages -d ."
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "json-server": "^0.17.3",
    "typescript": "^5.1.3"
  }
}
